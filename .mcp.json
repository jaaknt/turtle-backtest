{
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://claude@hetzner:5432/trading"
      ],
      "env": {
        "PGPASSWORD": "${DB_CLAUDE_PASSWORD}"
      }
    }
  }
}