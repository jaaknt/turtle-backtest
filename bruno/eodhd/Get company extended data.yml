info:
  name: Get company extended data
  type: http
  seq: 2

http:
  method: GET
  url: https://eodhd.com/api/us-quote-delayed?s=AMZN.US&api_token={{process.env.EODHD_API_KEY}}&fmt=json
  params:
    - name: s
      value: AMZN.US
      type: query
    - name: api_token
      value: "{{process.env.EODHD_API_KEY}}"
      type: query
    - name: fmt
      value: json
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
